const namespaces = 'auth';

const Types = {
  USER_LOGIN_REQUEST: `${namespaces}/USER_LOGIN_REQUEST`,
  REGISTER_REQUEST: `${namespaces}/REGISTER_REQUEST`,
  RESET_PASSWORD_REQUEST: `${namespaces}/RESET_PASSWORD_REQUEST`,
};

export default Types;
